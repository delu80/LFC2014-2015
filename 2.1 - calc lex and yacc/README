calc example working

use make to compile
the process of make follows the following flow:

yacc –d bas.y				# create y.tab.h, y.tab.c
lex bas.l				# create lex.yy.c
cc lex.yy.c y.tab.c –obas.exe		# compile/link

